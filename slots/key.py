from sandbox import ext_particles

##################
### import key ###
##################
# TODO: set to frozen order to avoid bugs after new versions

keys = {}
for index, i in enumerate(ext_particles):
    keys[i] = index
